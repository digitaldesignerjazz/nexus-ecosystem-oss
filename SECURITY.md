# Security

Do not open issues that contain live keys, `tskey-*` values, wallet seeds, Kyber material, or skilllogin state.

If you believe a secret landed in this repository, rotate the credential first, then open a private advisory to the owner.

Funnel exposes a local port to the public internet. Only publish a service that has its own authentication.
